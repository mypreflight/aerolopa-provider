services:
  app:
    build:
      context: .
      target: development
    volumes:
      - .:/app
      - ./node_modules:/app/node_modules
    env_file:
      - .env
    ports:
      - '3001:3000'
    tty: true
